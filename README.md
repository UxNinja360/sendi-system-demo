
  # sendi.io System Demo

  This repository contains the Sendi system demo application.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
